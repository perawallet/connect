.idea
.vscode
node_modules

# misc
.DS_Store
npm-debug.log
.npm
pnpm-debug.log

# pnpm / tooling config — not needed by consumers
pnpm-lock.yaml
pnpm-workspace.yaml
.tool-versions
.npmrc
.github
.githooks
.gitleaksignore

# Secret-scan binary. CI downloads it outside the checkout; this is a backstop
# so it can never ship in the package again.
gitleaks
gitleaks.tar.gz
